Django==6.0.7
openpyxl==3.1.5

# Optional — only needed for the live API pull commands (manage.py
# import_google_ads / import_meta_ads). Not required for CSV import.
# google-ads==26.1.0
# facebook-business==21.0.0
